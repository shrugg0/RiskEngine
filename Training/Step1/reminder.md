Step 1 — classe Dado
Argomenti da studiare: classi in C++, costruttore, metodi, private/public, #include e header files .h + .cpp separati.
Output: una classe Dado con un metodo tira() che restituisce un numero da 1 a 6 usando <random> (non rand() — cercati perché <random> è superiore).
